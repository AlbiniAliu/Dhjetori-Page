/** Public folder paths — keep filenames aligned with `public/` (lowercase on disk for Linux deploys). */
export const PUBLIC_IMAGES = {
  hero: "/hero.jpg",
  cover: "/cover.jpg",
  about: "/about.jpg",
} as const;
